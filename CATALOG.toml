# Machine-readable entry for this repository. Aggregated into public-software/catalog.
# Edit here; `pub catalog sync` propagates description, topics and custom properties to GitHub.

[repo]
name        = "plugin-runtime"
ring        = "platform"          # spine | platform | system | domain | standards
layers      = ["L2"]
wave        = 1
tier        = "incubating"        # incubating | stable | core | archived
maintainers = ["maint-plugin-runtime"]
summary     = "WASM Component Model host, capability policy, plugin test-kit, plugin packaging."

# One [[component]] per crate, app or service this workspace publishes.
# planned: pub-plugin-host · pub-plugin-sdk · pub-plugin-testkit · pub-plugin-pack
#
# [[component]]
# crate     = "pub-plugin-runtime-example"
# kind      = "lib"                # lib | app | service | plugin | spec
# ledger    = "<ledger entry name>"
# readiness = "none"               # shipped | partial | seed | none
# effort    = 3
# specs     = []
# provides  = ["public:area/iface@1"]
# requires  = []

[[component]]
crate     = "pub-plugin-runtime-policy"
kind      = "lib"
ledger    = "plugin-runtime"
readiness = "seed"
effort    = 3
specs     = ["component-model-wit"]
provides  = []
requires  = []
